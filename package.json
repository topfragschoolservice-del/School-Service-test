{
  "name": "school-transport-management",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "backend:start": "npm --prefix backend run dev",
    "backend:prod": "npm --prefix backend start",
    "backend:install": "npm --prefix backend install",
    "deploy:build": "npm install && npm run backend:install && npm run build",
    "deploy:start": "npm --prefix backend start"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
